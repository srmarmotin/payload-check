<button id="clean-table">Clean Table</button>
<table>
    <thead>
        <tr>
            <th>Remove</th>
            <th>Key</th>
            <th>Assertion</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
        {{#rows}}
        <tr class="table-results" data-row="{{key}}">
            <td class="remove-row" data-key="{{key}}">X</td>
            <td>{{key}}</td>
            <td>{{values.label}}</td>
            <td>{{values.value}}</td>
        </tr>
        {{/rows}}
    </tbody>
</table>

<!-- Single error popup for all rows -->
<div id="error-popup" class="error-popup">
    <div class="error-popup-content">
        <strong>Assertion Failed</strong><br>
        Expected: <span class="expected-value"></span><br>
        Received: <span class="received-value"></span>
    </div>
</div>